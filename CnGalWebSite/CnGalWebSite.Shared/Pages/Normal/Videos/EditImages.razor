@page "/videos/editimages/{Id}"
@attribute [Authorize]

@inject IDataCacheService _dataCacheService

<MakeSureInWASMTip>
    <CnGalWebSite.Shared.Pages.Webs.Videos.EditImages Id="@Id" />
</MakeSureInWASMTip>

@code{
    [Parameter]
    public string Id { get; set; }
    protected override void OnInitialized()
    {
        //清空当前缓存
        _dataCacheService.VideoIndexPageCatche.Clean(Id);
    }
}
