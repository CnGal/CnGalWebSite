@page "/peripheries/editrelatedPeripheries/{Id}"

@attribute [Authorize]

@inject IDataCacheService _dataCacheService

<MakeSureInWASMTip>
    <CnGalWebSite.Shared.Pages.Webs.Peripheries.EditRelatedPeripheries Id="@Id" />
</MakeSureInWASMTip>

@code{
    [Parameter]
    public string Id { get; set; }
    protected override void OnInitialized()
    {
        //清空当前缓存
        _dataCacheService.PeripheryIndexPageCatche.Clean(Id);
    }
}
