<CgModal IsOpen="_isOpen" OnClose="Close" Title="规则说明">
    <ChildContent>
        <div class="cg-rules-dialog">
            <p>一、抽奖次数获取规则:</p>
            <p>活动期间，用户可通过完成指定任务积累积分。每满100积分可获得一次抽奖机会。</p>
            <br />
            <p>二、奖品发放及使用规则:</p>
            <p>中奖激活码可在"我的奖品"中查看，且仅限中奖用户本人使用。</p>
            <br />
            <p>本次活动严禁恶意刷奖行为。若发现用户存在此类行为，CnGal资料站有权取消其获奖资格。</p>
            <p>活动期间，用户所中奖项以最终收集的中奖信息为准。如因用户提供的信息不准确、错误或不完整，导致奖品无法正常发放，CnGal资料站将不承担相关责任。</p>
        </div>
    </ChildContent>
</CgModal>

@code {
    private bool _isOpen;

    public void Open()
    {
        _isOpen = true;
        StateHasChanged();
    }

    private Task Close()
    {
        _isOpen = false;
        return Task.CompletedTask;
    }
}
