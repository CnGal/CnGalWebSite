@* 向后兼容包装：直接委托给共享 CgAtmosphere 组件 *@
<CgAtmosphere BackgroundPicture="@BackgroundPicture" SmallBackgroundPicture="@SmallBackgroundPicture" />

@code {
    [Parameter] public string? BackgroundPicture { get; set; }
    [Parameter] public string? SmallBackgroundPicture { get; set; }
}
