@inject IUserService _userService

<AuthorizeView>
    <Authorized>
        <UserButton Class="me-4"/>
        <CnGalWebSite.Components.Buttons.MasaButton Icon="@IconType.Create.ToIconString()" Text="创建企划" Rounded Href="/project/edit?id=0"/>
    </Authorized>
    <NotAuthorized>
        <CnGalWebSite.Components.Buttons.MasaButton Icon="@IconType.Login.ToIconString()" Tooltip="登入" OnClick="_userService.Login" />
    </NotAuthorized>
</AuthorizeView>

@code {
}
