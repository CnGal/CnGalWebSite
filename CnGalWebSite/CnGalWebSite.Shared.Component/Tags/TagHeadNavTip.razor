
<MBreadcrumbs Items="@GetItems()"></MBreadcrumbs>

@code {
    [Parameter]
    public List<TagLevelViewModel> TagList { get; set; }

    public List<BreadcrumbItem> GetItems()
    {
        return TagList.Select(s => new BreadcrumbItem
            {
                Text = s.Name,
                Href = "/tags/index/" + s.Id
            }).ToList();
    }

}
